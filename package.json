{
  "name": "voicify",
  "version": "1.0.0",
  "packageManager": "yarn@4.9.2",
  "scripts": {
    "s": "clear && cd demo && python3 -m http.server 5000",
    "b": "tsup src/main.ts --format esm --dts && esbuild dist/main.js --bundle --minify --format=esm --platform=node --outfile=dist/main.mjs --keep-names --allow-overwrite"
  },
  "devDependencies": {
    "@swc/cli": "^0.7.8",
    "@swc/core": "^1.13.1",
    "esbuild": "^0.25.8",
    "tsup": "^8.5.0",
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "tesseract.js": "^6.0.1"
  }
}
